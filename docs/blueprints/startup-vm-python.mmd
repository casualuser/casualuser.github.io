%% Autogenerated startup-vm-python
graph TD
  user("User / Client<br/>(actor)")
  host("Virtual Machine (Linux)<br/>(compute)")
  nginx("Nginx<br/>(gateway)")
  app("Python App (Gunicorn)<br/>(service)")
  db("PostgreSQL<br/>(database)")
  redis("Redis<br/>(cache)")
  nginx --> app
  app --> db
  app --> redis
  subgraph vm-layer
    nginx
    app
    db
    redis
  end